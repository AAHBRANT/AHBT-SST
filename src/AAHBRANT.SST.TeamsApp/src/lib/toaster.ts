// Um único <Toaster> montado no AppShell (ver layout/AppShell.tsx) atende o app inteiro — cada tela
// só precisa do hook useSucessoToast (ver hooks/useSucessoToast.tsx) pra disparar notificações nele.
export const ID_TOASTER_GLOBAL = 'toaster-global-sst';
